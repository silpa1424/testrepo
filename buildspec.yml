version: 0.2
phases:
  install:
    runtime-versions:
      dotnet: 3.1
  build:
    commands:
      - echo "CD into source folder, because my source code is tidy"
artifacts:
  base-directory: outputDirectory
  files:
    - '**/*'






